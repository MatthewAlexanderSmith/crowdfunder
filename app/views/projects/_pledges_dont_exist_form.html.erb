<div id="pledges-dont-exist">
  <p>Pledge to this project:</p>
  <%= form_for [@project, @pledge], id: "new-pledge" do |f| %>
      <div class="field">
        <%= f.label :amount %>
        <%= f.number_field :amount %>
      </div>

      <div class="action">
        <%= f.submit %>
      </div>
  <% end %>
</div>